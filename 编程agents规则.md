# 编程 Agents 规则

> 用途：把本规则发给 AI 编程助手，作为通用开发约束。适用于桌面应用 / Web / 全栈项目。
> 来源：playerWCY 项目（Tauri 2 + Rust + 原生前端 + SQLite）全程开发沉淀。

## 一、开发工作流（每次改动必须遵守的顺序）

1. **文档先行**：改任何代码前，先更新项目的总 README（对应章节）与 CHANGELOG（新条目），再动代码。
2. **日志先行**：改动前在 `log/当天日期.log` 追加 `[时间] 进行：改动内容`；完成后改为 `[时间] 完成：改动内容`。当天文件不存在则新建，存在则追加到最后一行。编译/归档等里程碑动作自动写入日志。
3. **版本号三处同步**：凡升版本号，必须同时改 `package.json`、桌面打包配置（如 tauri.conf.json）、语言清单（如 Cargo.toml）。About 页显示取自打包配置——漏一处就是"版本号滞后"bug。
4. **构建归档**：每次可运行的产出按 `四位序号-日期-产品名` 入归档目录（如 `0001-20260901-playerWCY.app`），序号自动递增；归档目录名 = 项目建立日期-项目名-archive。
5. **测试闭环**：有单测的改动必须先跑测试再编译归档；修 bug 优先补回归测试。

## 二、编辑工具纪律（防"静默丢失"，血泪教训）

1. **同一文件的多处修改禁止并行执行**——并行编辑会互相覆盖，且工具可能报告成功但实际丢失。必须：多处小改 = 串行逐个执行；大改/重构 = 整体重写整个文件（单次写入最可靠）。
2. **修改后必须全文核验**：不能只 grep 本次改动的点，要核验该文件历史关键修改是否仍在（防止早前修改被覆盖丢失）。
3. **写入疑似失败（报 not found / 内容异常）时**：读文件确认真实状态，必要时用脚本（python 等）做精准插入或整体重写，不要反复重试同一编辑。
4. **大文件分块写入**：单次写入内容过大时分块（如先写前半、再在末尾追加后半）。

## 三、问题处理策略

1. **先诊断、后修复**：用户报 bug 时，先读代码定位根因、向用户确认分析（高置信可直接修），不要盲目改。
2. **遇错即换**：工具/命令第一次失败立即换替代方案，第二次再换，第三次拆小步骤；单操作不无限重试。
3. **修改前影响分析**：说明原代码设计意图、修改后的利弊、涉及文件清单、风险等级（高/中/低）；高风险先停下等用户确认。
4. **修复附带防复发**：每个 bug 修复写入 CHANGELOG（含根因），流程性漏洞写入 README 强制规则或 skill 记录。

## 四、代码质量清单（提交前逐项核对）

- [ ] 错误不静默：所有异步/IO 调用有空 catch 时必须至少 `console.error` / 日志输出，带中文上下文说明
- [ ] 数据库多语句写操作（级联删除、批量操作）必须包事务，失败整体回滚
- [ ] 数值边界 clamp：百分比等派生值统一限制在合法区间（如 0-100），前后端两侧都做
- [ ] 身份用稳定标识（如 file_path/UUID），不用数组索引；列表刷新后按标识重算位置
- [ ] 保存/读取时序：换源/刷新会重置状态的场景（如 video 换 src 重置进度），统一在元数据就绪事件（loadedmetadata 类）中恢复
- [ ] 关闭/退出前保存必须 `await` 写入完成后再销毁窗口
- [ ] 跨线程共享状态：原子变量或锁保护完整流程（不只保护单个读写）
- [ ] 系统资源（监听器/句柄）：优先监听"系统级对象"的属性变化再重绑到具体实例（如默认设备变化 → 迁移监听），并处理实例失效场景
- [ ] 异步 UI 交互：拖动/输入期间暂停自动回写（如 timeupdate 回写进度条），防止覆盖用户操作

## 五、技术备忘（macOS / Tauri / 前端专项）

1. **无 bundler 的 vanilla 前端**不能 `import from '@tauri-apps/...'`（裸模块名解析失败导致整个模块不加载、按钮全失灵）；必须启用 `withGlobalTauri` 用 `window.__TAURI__`。
2. **Tauri capabilities 权限**：窗口操作（hide/destroy/close）默认无权限，调用会被静默拒绝——用了什么 API 就要在 capabilities 声明对应权限，并检查调用返回的 Promise 是否被拒。
3. **CSS flex 布局**：弹性子项必须 `min-width: 0; min-height: 0` 才能正常收缩；底部固定区加 `flex-shrink: 0`——否则内容多时把操作栏挤出可视区（body overflow:hidden 裁掉）。
4. **WebKit 中 transform 层会被 `<video>` 合成层部分遮挡**：悬浮在视频上的元素做"悬停放大"用实际宽高变化，不用 `transform: scale`。
5. **macOS 系统音量**：快捷键调节的是 CoreAudio `VirtualMasterVolume`（vmvc），不是物理声道 VolumeScalar——监听错属性会"音量变了界面不动"（间歇性）。读写监听都以 vmvc 为主、声道值兜底；监听默认设备变化事件做重绑。
6. **Finder 定位文件**用 `open -R <path>`；文件可能已被移动，先校验存在性并给出友好提示。
7. **行首 `---` 等特殊行**在部分编辑工具中有写入异常风险，分隔线改用 `━━━` 等字符。

## 六、UI / UX 规范

1. **品牌一致**：改动不破坏既有品牌（配色/图标语言/主题），装饰克制——层级、间距、状态反馈优先于视觉花活。
2. **可达性**：全局 `:focus-visible` 焦点环；`prefers-reduced-motion` 降级动画；最小字号不低于 12px。
3. **状态可感知**：主操作按钮视觉权重高于次操作；当前选中/生效项有明显指示（角标/指示条）；空状态给图标+引导文案；错误状态给原因+下一步建议。
4. **交互一致性**：快捷键不与输入冲突（焦点在输入框时全局快捷键让位）；回车确认后失焦归还快捷键；危险操作（删除）二次确认并说明影响范围。
5. **图标**：统一线条风格 SVG（stroke=currentColor 随主题变色），不用 emoji 当图标。

## 七、沟通与交付规范

1. 全程中文回复与中文代码注释；输出精炼、先结论后细节。
2. 每次交付附：改动清单（文件+行级链接）、验证方法、已知风险；多方案时列表格对比优劣，等用户选定再动手。
3. git 提交信息格式：`v版本号 描述` 或 `修复/新增：描述`；不擅自 push，推送时机听用户。
4. 网络受限环境：优先国内镜像（npm/cargo）；HTTPS 被间歇阻断时 SSH(22) 常可用；凭证类操作失败（钥匙串被沙盒拦截）及时转人工路径（如 GitHub Desktop 手动推送）并给用户两步内的操作指引。

## 八、长期记忆

- 确认有效的修复模式 → 写入项目 `.claude/skills/<主题>/SKILL.md`（场景/根因/位置/模板/关键词/验证法六要素），后续同类任务直接复用。
- 流程性规则 → 同步进项目 README"修改前必读"，保证任何新会话都能看到。
